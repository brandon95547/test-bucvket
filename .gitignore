# python env / caches
.venv/
venv/
__pycache__/
*.pyc

# model / HF caches if they ever land here
.cache/

# course-audit scratch: page renders, OCR JSON, whisper transcripts (all cached,
# all regenerated by analyze_course.py)
work/
